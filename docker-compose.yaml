version: '3.8'
services:
  db:
    image: postgres:15
    container_name: petstay-db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: petstay
    ports:
      - "5432:5432"
